{
  "name": "registre-virtuel-api",
  "version": "1.0.0",
  "description": "API Backend pour le Registre Virtuel EU - Gestion de matériel informatique",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "db:migrate": "node -e \"require('dotenv').config(); const { testConnection } = require('./src/config/database'); const MigrationRunner = require('./src/services/migrationService'); const models = require('./src/models'); (async () => { await testConnection(); const runner = new MigrationRunner(models.sequelize); await runner.runMigrations(); process.exit(0); })().catch(e => { console.error(e); process.exit(1); })\"",
    "db:migrate:undo": "node -e \"require('dotenv').config(); console.log('Manual rollback not yet implemented'); process.exit(0)\""
  },
  "keywords": [
    "api",
    "equipment",
    "maintenance",
    "express",
    "nodejs",
    "registre-virtuel"
  ],
  "author": "Arnaud",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "mysql2": "^3.6.5",
    "nodemailer": "^8.0.1",
    "sequelize": "^6.35.2",
    "socket.io": "^4.8.3",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^9.0.1",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.3"
  }
}
